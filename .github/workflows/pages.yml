# Publica en GitHub Pages solo el sitio: la portada, la plantilla y los reportes.
# Sube los archivos tal cual, sin Jekyll: Jekyll omitiría plantilla/_ds/ (los estilos
# del diseño) y la plantilla se vería sin recuadros ni tipografías.
# Requiere Settings → Pages → Source: «GitHub Actions».
name: Publicar sitio

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'plantilla/**'
      - 'reportes/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publicar:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.despliegue.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7

      - name: Armar el sitio
        run: |
          mkdir -p _site
          cp -r index.html plantilla reportes _site/

      - uses: actions/configure-pages@v6

      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - id: despliegue
        uses: actions/deploy-pages@v5
